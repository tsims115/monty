#include "monty.h"
/**
 * nop - does not do anything
 * @stack: stack not to use
 * @line_number: line_number not to use
 */

void nop(stack_t **stack, unsigned int line_number)
{
	( void ) stack;
	( void ) line_number;
}
